- name: nginx playbook
  hosts: web
  become: yes
  tasks:
  - name: installing nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: start the service
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes