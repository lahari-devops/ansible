- name: loops
  hosts: web
  become: yes
  tasks:
  - name: looping
    ansible.builtin.package:
      name: "{{item.name}}"
      state: "{{item.state}}"
    loop:
    - { name: 'git', state: 'present' } 
    - { name: 'zip', state: 'absent' }